# coding: utf-8

# As tuplas são imutáveis. Uma vez declaradas, não conseguimos alterar seus valores.
coordenadas = (12, 34)
print(coordenadas[1]) # imprime a segunda posição da tupla 'coordenadas' (12)

coordenaras_lista = [(4, 5), (10, -3), (39, 22)] # lista de tuplas